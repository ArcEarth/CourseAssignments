<UserControl x:Class="TripAdviserDesktopClient.Pages.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <StackPanel MinWidth="200">
                <TextBlock Text="TODO: add content" />
                <DataGrid Name="CitiesDataGrid" DataContext="{Binding}" ItemsSource="{Binding}"/>
                <DataGrid Name="FlightsDataGrid" DataContext="{Binding}" ItemsSource="{Binding}"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
